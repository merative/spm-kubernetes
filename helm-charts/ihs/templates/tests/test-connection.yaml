apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ihs.fullname" . }}-test-connection"
  labels:
{{ include "ihs.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "ihs.fullname" . }}:{{ .Values.global.ihs.servicePort }}']
  restartPolicy: Never
